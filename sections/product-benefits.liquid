{% comment %}
  Product Benefits Section
  Displays three benefit columns with full images and text overlays.
{% endcomment %}

<section class="product-benefits" data-section-id="{{ section.id }}">
  <div class="product-benefits__container">
    {% for block in section.blocks %}
      <div
        class="product-benefits__column"
        {{ block.shopify_attributes }}
        data-block-id="{{ block.id }}">
        {% if block.settings.image != blank %}
          <div class="product-benefits__image-wrapper">
            <img
              src="{{ block.settings.image | image_url: width: 800 }}"
              alt="{{ block.settings.heading | escape }}"
              loading="lazy"
              class="product-benefits__image"
              width="800"
              height="800">
            <div class="product-benefits__gradient"></div>
          </div>
        {% endif %}

        <div class="product-benefits__content">
          {% if block.settings.heading != blank %}
            <h3 class="product-benefits__heading">
              {{ block.settings.heading }}
            </h3>
          {% endif %}

          {% if block.settings.description != blank %}
            <p class="product-benefits__description">
              {{ block.settings.description }}
            </p>
          {% endif %}
        </div>
      </div>
    {% endfor %}
  </div>
</section>

{% stylesheet %}
  .product-benefits {
    width: 100%;
  }

  .product-benefits__container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    max-width: 1600px;
    margin: 0 auto;
    padding: 100px 40px 0;
  }

  .product-benefits__column {
    position: relative;
    overflow: hidden;
  }

  .product-benefits__image-wrapper {
    position: relative;
    width: 100%;
    height: 721px;
  }

  .product-benefits__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .product-benefits__gradient {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 300px;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.8) 0%, rgba(0, 0, 0, 0) 100%);
    pointer-events: none;
  }

  .product-benefits__content {
    position: absolute;
    left: 26px;
    right: 26px;
    bottom: 0;
    top: 0;
    color: #FFFFFF;
    z-index: 10;
    pointer-events: none;
  }

  .product-benefits__heading {
    font-family: 'Sweet Sans Pro'
    , sans-serif;
    font-weight: 500;
    font-size: 18px;
    line-height: normal;
    letter-spacing: -0.18px;
    text-transform: uppercase;
    margin: 0;
    position: absolute;
    bottom: 171px;
    left: 0;
    right: 0;
  }

  .product-benefits__description {
    font-family: 'Jokker'
    , serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 1.5;
    margin: 0;
    position: absolute;
    top: 570px;
    left: 0;
    right: 0;
  }

  /* Responsive design */
  @media screen and (max-width: 1024px) {
    .product-benefits__container {
      grid-template-columns: 1fr;
      padding: 60px 20px 0;
    }

    .product-benefits__column {
      margin-bottom: 40px;
    }

    .product-benefits__column:last-child {
      margin-bottom: 0;
    }

    .product-benefits__image-wrapper {
      height: 600px;
    }

    .product-benefits__content {
      left: 20px;
      right: 20px;
    }

    .product-benefits__heading {
      font-size: 16px;
      bottom: 140px;
    }

    .product-benefits__description {
      font-size: 14px;
      top: 480px;
    }
  }

  @media screen and (max-width: 768px) {
    .product-benefits__container {
      padding: 40px 16px 0;
    }

    .product-benefits__image-wrapper {
      height: 500px;
    }

    .product-benefits__content {
      left: 16px;
      right: 16px;
    }

    .product-benefits__heading {
      font-size: 15px;
      bottom: 120px;
    }

    .product-benefits__description {
      font-size: 13px;
      line-height: 1.4;
      top: 390px;
    }
  }

  @media screen and (max-width: 480px) {
    .product-benefits__container {
      padding: 30px 12px 0;
    }

    .product-benefits__image-wrapper {
      height: 450px;
    }

    .product-benefits__heading {
      font-size: 14px;
      bottom: 100px;
    }

    .product-benefits__description {
      font-size: 12px;
      top: 350px;
    }
  }
{% endstylesheet %}

{% schema %}
  {
    "name": "Product benefits",
    "tag": "section",
    "class": "section",
    "max_blocks": 3,
    "settings": [],
    "blocks": [
      {
        "type": "benefit",
        "name": "Benefit column",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          }, {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Benefit heading"
          }, {
            "type": "textarea",
            "id": "description",
            "label": "Description",
            "default": "Describe the benefit here."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Product benefits",
        "blocks": [
          {
            "type": "benefit",
            "settings": {
              "heading": "Unique Odor-Control Formula",
              "description": "Clinically backed and powered by odor-eliminating bacterial technology. Targets the bacteria that cause smell — not sweat — for freshness that starts before your deodorant."
            }
          }, {
            "type": "benefit",
            "settings": {
              "heading": "No Residue. No Scent. No Interference.",
              "description": "The wipe-off formula leaves your skin clean, balanced, and scent-free — ready for deodorant or perfume without overlap or irritation."
            }
          }, {
            "type": "benefit",
            "settings": {
              "heading": "For Life, Sweat & Everything In Between",
              "description": "From yoga to long workdays, Baudie keeps odor-causing bacteria in check — so you stay fresh, confident, and completely unbothered."
            }
          }
        ]
      }
    ]
  }
{% endschema %}